#include<bits/stdc++.h>
using namespace std;

int main(){
    /*
                |----->Object which stores data
                |
    STL =    Containers       + Algo    +  Iterators------> Object which points to an element of a container
                                |
                                |------> Procedure to process data


    Containers-->  1. Sequence Containers --(Linear fashion)
                         data are stored in linearly--vector, list, Dequeue
                   2. Associative Containers --(Direct Access)
                         data are stored in Binary Search Tree --Set/Multiset & Map/Multimap
                   3. Derived Containers --(Real World Modelling)
                         data are stored in stack(LIFO), queues(FIFO), priority-queue

    
    */
    return 0;
}